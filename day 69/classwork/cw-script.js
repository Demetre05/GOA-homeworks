/* ფუნქცია გამოიყენება კოდის ბლოკისშესაქმნელად, რომელიც შეგვეძლება ქუნქციის სახელით მრავალჯერ გამოვიძახოთ და არ მოგვიწიოს
მთლიანი ბლოკის ყოველ ჯერზე დაწერა, რაც კოდს გვიმოკლებს */

// გამოიყენება keyword function

function sum(a, b) {
    return a + b
}

console.log(sum(5, 7))

// return - საჭიროა ფუნქციის კოდის ბლოკის საბოლოო პასუხის გამოსატანად, return-ის შემდეგ კოდის ბლოკში დაწერილი კოდები იგნორდება

function bol(x) {
    return x > 10
}

console.log(bol(12))